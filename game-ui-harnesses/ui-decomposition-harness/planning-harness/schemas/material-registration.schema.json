{
  "type": "object", "additionalProperties": false,
  "required": ["parts", "issues"],
  "properties": {
    "issues": {"type": "array", "items": {"type": "string"}},
    "parts": {"type": "array", "maxItems": 128, "items": {
      "type": "object", "additionalProperties": false,
      "required": ["objectId", "fitMode", "fragments"],
      "properties": {
        "objectId": {"type": "string"},
        "fitMode": {"type": "string", "enum": ["contain", "frame-bounds"]},
        "fragments": {"type": "array", "minItems": 1, "maxItems": 32, "items": {
          "type": "object", "additionalProperties": false,
          "required": ["sourceBox", "targetBox"],
          "properties": {
            "sourceBox": {"type": "array", "minItems": 4, "maxItems": 4, "items": {"type": "integer", "minimum": 0}},
            "targetBox": {"type": "array", "minItems": 4, "maxItems": 4, "items": {"type": "integer", "minimum": 0}}
          }
        }}
      }
    }}
  }
}
